<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>LIST</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="/send_url.js"></script>
</head>

<body>
    <header>
        <a href="/" class="header-logo">LIST</a>
    </header>
    <h1>name <%= name %>
    </h1>
    <h1>id <%= id %>
    </h1>
    <h1>query <%= query %>
    </h1>
    <div class="container">
        <div class="container-header">
            <h1>Shopping List</h1>
            <a href="/admin/new" class="new-button">+ Add item</a>
        </div>
        <div class="index-table-wrapper">
            <div class="table-head">
                <span class="id-column">ID</span>
                <span>Item</span>
            </div>
            <ul class="table-body">
                <li>
                    <% if(item){ %>
                        <div class="item-data">
                            <span class="id-column">
                                <%= item.id %>
                            </span>
                            <span class="name-column">
                                <%= item.name %>
                            </span>
                        </div>
                        <div class="item-menu">
                            <form action="/admin/delete/<%= item.id %>" method="post">
                                <input type="submit" value="Delete">
                            </form>
                            <a href="/admin/edit/<%= item.id %>">Edit</a>
                        </div>
                        <% } else { %>
                            <p>Data id <%= id %> dan nama <%= name %> gak ada </p>
                            <% } %>
                </li>
            </ul>
        </div>
    </div>
</body>

</html>